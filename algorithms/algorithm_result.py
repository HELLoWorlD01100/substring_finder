class AlgorithmResult:
    def __init__(self, found_indexes: [], current_indexes: (), is_over: bool):
        self.found_indexes = found_indexes
        self.current_indexes = current_indexes
        self.is_over = is_over
